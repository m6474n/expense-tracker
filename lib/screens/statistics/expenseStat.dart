import 'package:flutter/material.dart';

class ExpenseStatScreen extends StatelessWidget {
  const ExpenseStatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Column(children: [Text("Expense")],),
    );
  }
}